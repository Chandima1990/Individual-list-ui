import { AddressListStringPipe } from './address-list-string.pipe';

describe('AddressListStringPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressListStringPipe();
    expect(pipe).toBeTruthy();
  });
});
